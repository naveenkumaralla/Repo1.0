# KVM Manager configuration

LOG_DIR="./logs"
BACKUP_DIR="./backups"
SNAPSHOT_PREFIX="snap"

# Change to "true" if dashboard should only show running VMs by default.
FILTER_RUNNING_ONLY=false

# Default connection is inherited from virsh. Set LIBVIRT_DEFAULT_URI outside
# this file if you need qemu:///system or another URI.
